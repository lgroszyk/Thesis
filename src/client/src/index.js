import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';

// Renderuje kontener na interfejs użytkownika strony.
ReactDOM.render(<App />, document.getElementById('root'));
